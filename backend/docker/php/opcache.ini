; OPcache production tuning — critical for Symfony performance

; Activation
opcache.enable         = 1
opcache.enable_cli     = 0

; Memory — 256MB for a mid-size Symfony project
opcache.memory_consumption     = 256
opcache.interned_strings_buffer = 16
opcache.max_accelerated_files   = 20000

; Validation — disable in prod (files never change inside the container)
opcache.validate_timestamps = 0
opcache.revalidate_freq     = 0

; Optimisation
opcache.fast_shutdown         = 1
opcache.huge_code_pages       = 1
opcache.preload_user          = app
; opcache.preload            = /var/www/html/config/preload.php  # Symfony preload

; JIT (PHP 8+) — tracing mode, optimal for REST APIs
opcache.jit                   = tracing
opcache.jit_buffer_size       = 64M
